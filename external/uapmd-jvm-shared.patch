diff --git a/source/CMakeLists.txt b/source/CMakeLists.txt
index cf74a40d..e9b88226 100644
--- a/source/CMakeLists.txt
+++ b/source/CMakeLists.txt
@@ -264,12 +264,12 @@ add_subdirectory(uapmd)
 add_subdirectory(uapmd-file)
 add_subdirectory(uapmd-data)
 add_subdirectory(uapmd-engine)
-add_subdirectory(uapmd-c-api)
 if(EMSCRIPTEN AND TARGET liblocate)
     target_compile_definitions(liblocate PRIVATE LIBLOCATE_DISABLE_DLOPEN)
 endif()
 
 add_subdirectory(tools)
+add_subdirectory(uapmd-c-api)
 
 # Tests
 if (NOT ANDROID)
diff --git a/source/remidy-gui/include/remidy-gui/remidy-gui.hpp b/source/remidy-gui/include/remidy-gui/remidy-gui.hpp
index 0eefddee..140f5104 100644
--- a/source/remidy-gui/include/remidy-gui/remidy-gui.hpp
+++ b/source/remidy-gui/include/remidy-gui/remidy-gui.hpp
@@ -6,6 +6,5 @@
 #include <remidy-gui/detail/ContainerWindow.hpp>
 #if defined(__ANDROID__)
 #include <remidy-gui/detail/AndroidContainerWindow.hpp>
-#else
-#include <remidy-gui/detail/GLContextGuard.hpp>
 #endif
+#include <remidy-gui/detail/GLContextGuard.hpp>
diff --git a/source/uapmd-c-api/CMakeLists.txt b/source/uapmd-c-api/CMakeLists.txt
index b9cb7053..7eaaf272 100644
--- a/source/uapmd-c-api/CMakeLists.txt
+++ b/source/uapmd-c-api/CMakeLists.txt
@@ -21,7 +21,7 @@ set(UAPMD_C_API_SOURCES
     src/uapmd-c-app.cpp
 )
 
-add_library(uapmd-c-api STATIC ${UAPMD_C_API_HEADERS} ${UAPMD_C_API_SOURCES})
+add_library(uapmd-c-api SHARED ${UAPMD_C_API_HEADERS} ${UAPMD_C_API_SOURCES})
 
 target_include_directories(uapmd-c-api
     PUBLIC
@@ -40,6 +40,14 @@ target_link_libraries(uapmd-c-api
         remidy-gui
 )
 
+# Pull in AppModel and related app-layer implementations.
+# On desktop the sources are compiled into an OBJECT library; on Android into a SHARED lib.
+if(ANDROID)
+    target_link_libraries(uapmd-c-api PRIVATE uapmd-app)
+elseif(NOT IOS AND NOT EMSCRIPTEN)
+    target_link_libraries(uapmd-c-api PRIVATE uapmd-app-objects)
+endif()
+
 # These modules use PRIVATE includes, so we must re-add their paths here.
 target_include_directories(uapmd-c-api
     PRIVATE
diff --git a/source/uapmd-c-api/include/c-api/uapmd-c-api.h b/source/uapmd-c-api/include/c-api/uapmd-c-api.h
index d932e922..f39f9abf 100644
--- a/source/uapmd-c-api/include/c-api/uapmd-c-api.h
+++ b/source/uapmd-c-api/include/c-api/uapmd-c-api.h
@@ -93,7 +93,8 @@ UAPMD_C_EXPORT uint32_t uapmd_plugin_host_catalog_entry_count(uapmd_plugin_host_
 UAPMD_C_EXPORT bool uapmd_plugin_host_get_catalog_entry(uapmd_plugin_host_t host, uint32_t index,
                                                           char* format_buf, size_t format_buf_size,
                                                           char* plugin_id_buf, size_t plugin_id_buf_size,
-                                                          char* display_name_buf, size_t display_name_buf_size);
+                                                          char* display_name_buf, size_t display_name_buf_size,
+                                                         char* vendor_buf, size_t vendor_buf_size);
 
 UAPMD_C_EXPORT void uapmd_plugin_host_save_catalog(uapmd_plugin_host_t host, const char* path);
 UAPMD_C_EXPORT void uapmd_plugin_host_perform_scanning(uapmd_plugin_host_t host, bool rescan);
diff --git a/source/uapmd-c-api/src/uapmd-c-api.cpp b/source/uapmd-c-api/src/uapmd-c-api.cpp
index 01b3f9e2..6c066f6b 100644
--- a/source/uapmd-c-api/src/uapmd-c-api.cpp
+++ b/source/uapmd-c-api/src/uapmd-c-api.cpp
@@ -262,7 +262,8 @@ uint32_t uapmd_plugin_host_catalog_entry_count(uapmd_plugin_host_t host) {
 bool uapmd_plugin_host_get_catalog_entry(uapmd_plugin_host_t host, uint32_t index,
                                           char* format_buf, size_t format_buf_size,
                                           char* plugin_id_buf, size_t plugin_id_buf_size,
-                                          char* display_name_buf, size_t display_name_buf_size) {
+                                          char* display_name_buf, size_t display_name_buf_size,
+                                          char* vendor_buf, size_t vendor_buf_size) {
     if (tl_catalog.empty())
         tl_catalog = H(host)->pluginCatalogEntries();
     if (index >= tl_catalog.size())
@@ -271,6 +272,7 @@ bool uapmd_plugin_host_get_catalog_entry(uapmd_plugin_host_t host, uint32_t inde
     copy_string(entry.format(), format_buf, format_buf_size);
     copy_string(entry.pluginId(), plugin_id_buf, plugin_id_buf_size);
     copy_string(entry.displayName(), display_name_buf, display_name_buf_size);
+    copy_string(entry.vendorName(), vendor_buf, vendor_buf_size);
     return true;
 }
 
diff --git a/source/uapmd-c-api/src/uapmd-c-data.cpp b/source/uapmd-c-api/src/uapmd-c-data.cpp
index 0d1f9917..1275c92b 100644
--- a/source/uapmd-c-api/src/uapmd-c-data.cpp
+++ b/source/uapmd-c-api/src/uapmd-c-data.cpp
@@ -5,6 +5,12 @@
 #include <cstring>
 #include <string>
 #include <vector>
+#include "c-api-internal.h"
+
+/* ── AudioFileReader ownership registry (declared extern in c-api-internal.h) */
+std::mutex s_reader_mutex;
+std::unordered_map<uapmd::AudioFileReader*,
+                   std::unique_ptr<uapmd::AudioFileReader>> s_owned_readers;
 
 /* ── Cast helpers ─────────────────────────────────────────────────────────── */
 
