/* stub_main.c
 * Empty entry point for the Emscripten module.
 * All public API is exported via EXPORTED_FUNCTIONS; main() just satisfies
 * the linker requirement for an executable target.
 */
int main(void) { return 0; }
